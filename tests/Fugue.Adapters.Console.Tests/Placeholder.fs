namespace Fugue.Adapters.Console.Tests

/// Placeholder — matches src/-side Placeholder.fs.
/// Removed by Phase 2 when foundation-type tests land
/// (T011 SafeTextPropertyTests, T012 StyleTests, T013 ContextTests, T013a Style.ofMarkupHint cases).
module internal Placeholder =
    let internal sentinel : unit = ()
